Chat Java 

Pour lancer le serveur -> Serveur/scr/Serveur.class
Pour lancer le client -> Client/scr/Interface.class

Si problème avec la sauvegarde, il faut changer le path dans le fichier Serveur/client.java et recompiler. 
Le fichier sauvegarde se trouve  la racine du dossier, sous le nom de « SaveChat ».

